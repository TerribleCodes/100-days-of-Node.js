# Arrays

```javascript
var cars = ["supra", "r34", "rx7", "EK9", "AE86"]

// console.log(cars[0])

//Pushes an item to last spot
cars.push("mx5")
for(var i=0; i<cars.length; i++){
    console.log(cars[i])
}

//Removes an item from the last spot
cars.pop()

console.log(cars.sort())
console.log(cars.reverse())
```